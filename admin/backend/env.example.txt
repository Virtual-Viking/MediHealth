# Admin auth
ADMIN_DEFAULT_EMAIL=admin@example.com
# Use ADMIN_PASSWORD_HASH for production; keep ADMIN_DEFAULT_PASSWORD only for local dev
ADMIN_PASSWORD_HASH=
ADMIN_DEFAULT_PASSWORD=changeme-local
ADMIN_JWT_SECRET_KEY=dev-secret
ADMIN_ACCESS_TOKEN_EXPIRE_MINUTES=60
ADMIN_PORT=8001
ADMIN_CORS_ORIGINS=http://localhost:3001,http://localhost:3000

# Database (shared with main app)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/medilink

# Grafana / Cloud Monitoring
GRAFANA_URL=http://localhost:3100
GRAFANA_API_KEY=put-your-grafana-api-key-here
GRAFANA_DS_UID=P3BE906CE9E430760

# GCP Monitoring (used by admin monitoring endpoints)
GCP_PROJECT_ID=your-gcp-project-id
# Optional location to scope some queries
GCP_LOCATION=
# Service account key path (if not using ADC defaults)
GOOGLE_APPLICATION_CREDENTIALS=/abs/path/to/your/key.json

# Prometheus (used by /admin/monitoring/prom/summary)
PROMETHEUS_URL=http://localhost:9090

